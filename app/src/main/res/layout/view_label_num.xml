<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/label_height">
    <TextView
        android:id="@+id/label_num_title"
        style="@style/LabelText.Left"/>
    <EditText
        android:layout_width="90dp"
        android:id="@+id/label_num_content"
        style="@style/LabelText.Right"
        android:layout_toRightOf="@id/label_num_title"
        android:background="@android:color/transparent"
        android:inputType="numberDecimal"/>
    <TextView
        android:id="@+id/label_num_unit"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_toRightOf="@id/label_num_content"
        style="@style/LabelText"/>
    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:background="@color/light_gray"
        android:layout_alignParentBottom="true"/>
</RelativeLayout>